# GuardFoundry Web (placeholder)

Not built out in v1. Seam only.

The GuardFoundry UI lands in a later phase (see `foundry_product_blueprint.md` §20).
Until then this package exists purely so the pnpm workspace graph is stable and
import-boundary checks have a consistent target.
